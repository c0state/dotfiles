<!--
  Claude Code user memory (symlinked to ~/.claude/CLAUDE.md).

  Canonical, agent-agnostic instructions live in ~/.dotfiles/AGENTS.md so any
  agent/tool can consume them. This file only carries Claude-specific bits:
  the @import below, plus any future Claude-only directives.

  Verify it's actually loaded with the /memory command in Claude Code.
-->
@~/.dotfiles/AGENTS.md

@~/.claude/RTK.md
